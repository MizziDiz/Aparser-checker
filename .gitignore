# рабочие данные (конфиг, состояние, логи, журнал отправок, дампы) — в data/
data/

# legacy-расположение (на случай старых файлов в корне) — не коммитить
aparser_monitor.config.json
aparser_monitor.state.json
aparser_monitor.log*
aparser_sent.jsonl
ui_dumps/
ui_dump.html
ui_dump.png
ui_cards.json
ui_first_cards.html

# кэш Python
__pycache__/
*.pyc

# виртуальное окружение (на контроллере/сервере)
.venv/
venv/

# сборка PyInstaller (редактор настроек)
build/
dist/
*.spec
aparser-config.exe

# артефакты кейгена (генерятся при запуске пайплайна)
keygen/parser_batches/
query_dedupe.sqlite

# рабочие данные харвеста
data/harvest/

# ИНФРА-СЕКРЕТЫ (реальные LAN-IP/роли/пути к кредам) — НЕ коммитить.
# harvest_runner.py вынесен в паблик-версию: инфра вынесена в data/nodes/harvest_node.json
# (см. harvest_node.example.json). Реальный конфиг узла — ниже, gitignored.
data/nodes/harvest_node.json
aparser-guard.bat
HANDOFF.md
data/ops/
.env
.smbcreds*
