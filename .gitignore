# рабочие данные (конфиг, состояние, логи, журнал отправок, дампы) — в data/
data/

# legacy-расположение (на случай старых файлов в корне) — не коммитить
aparser_monitor.config.json
aparser_monitor.state.json
aparser_monitor.log*
aparser_sent.jsonl
ui_dumps/
ui_dump.html
ui_dump.png
ui_cards.json
ui_first_cards.html

# кэш Python
__pycache__/
*.pyc

# виртуальное окружение (на контроллере/сервере)
.venv/
venv/

# сборка PyInstaller (редактор настроек)
build/
dist/
*.spec
aparser-config.exe

# артефакты кейгена (генерятся при запуске пайплайна)
keygen/parser_batches/
query_dedupe.sqlite

# автономный раннер добычи — содержит LAN-IP узла и путь к SMB-кредам (инфра)
harvest_runner.py
data/harvest/

# инфра/операционка — содержат реальные LAN-IP, роли узлов, пути к кредам. НЕ коммитить.
# (gitignore не поддерживает inline-комментарии — паттерны отдельными строками)
harvest_healthcheck.py
harvest_seedmine.py
aparser-guard.bat
HANDOFF.md
data/ops/
.env
.smbcreds*
