@echo off
rem Локальный монитор A-Parser (рабочий проход: чтение очереди + heartbeat/тревоги + autosend).
rem НУЖЕН ТОЛЬКО в АВТОНОМНОМ режиме (узел сам себя мониторит).
rem При ЦЕНТРАЛИЗОВАННОМ контроле НЕ регистрировать — мониторинг ведёт контроллер,
rem иначе будут двойные уведомления «завершено». Лог — data\monitor.out.log.
cd /d %~dp0
py aparser_monitor_ui.py >> data\monitor.out.log 2>&1
